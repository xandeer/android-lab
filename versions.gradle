def app_versions = [:]
app_versions.code = 1
app_versions.major = 0
app_versions.minor = 1
app_versions.maintenance = 1
app_versions.name = "${app_versions.major}.${app_versions.minor}.${app_versions.maintenance}"
ext.app_versions = app_versions
